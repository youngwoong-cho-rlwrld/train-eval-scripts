# macOS
.DS_Store

# Python
__pycache__/
*.py[cod]
*.egg-info/

# Editor / IDE
.vscode/
.idea/
*.swp

# Local runtime artifacts (small)
wandb/
logs/
*.out
*.err
*.log

# Training artifacts (large — model weights, optimizer state, RNG)
checkpoints/
checkpoints.*/

# Eval artifacts (videos + per-run JSON)
eval_results/
results.json

# Auto-generated per-run config (run.sh regenerates this via heredoc each run)
data_config.yaml

# Aggregated experiment results (Phase 3 output of run.sh)
results.json
